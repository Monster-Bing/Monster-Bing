法焙客封闭阿爸v
class Solution(object):
    def removeDuplicates(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        n = len(nums)
        i = 0
        while i < n:
            j = i + 1
            while j < n and nums[i] == nums[j]:
                del nums[j]
                n = len(nums)
            i += 1
